# Functional SIH26106 Validation Notes

The updated requirements preview initially showed **20 available**, **1 waiting for live services**, and **1 not built**. The completed high-risk owner-alert workflow moved the final alert item to available; the checklist now has **21 available**, **1 waiting for live services**, and **0 not built**. The available rows include local URL and attachment review, consent-based approximate IP geolocation and map/heatmap display, private similar-case comparison, role-aware administration, evidence-based reports, owner alerts, and the protected analysis workflow.

The remaining waiting connection is the grouped AbuseIPDB, VirusTotal, and PhishTank integration. High-risk owner alerts use the built-in project-owner notification channel; custom recipient channels remain outside the current scope. These statuses intentionally avoid presenting unsupported external reputation results as live data.

The development server was restarted after the changes. The final validation suite passed with 14 test files and 17 tests, followed by successful TypeScript checking and a production build. The requirements screen was checked at desktop and mobile sizes. End-to-end browser verification of the new geolocation approval, administrator role update, and high-risk alert timeline remains pending until a real uploaded email case is available; no fabricated case data was created for that purpose. The build continues to report non-blocking asset-at-runtime and bundle-size warnings.

The Location Map preview was rechecked after its empty-state revision. It now presents a three-step workflow: choose a saved private case, confirm that an extracted public source IP exists, and explicitly approve the approximate location lookup. The light-theme case-list override was also checked so saved case numbers and source-IP instructions remain readable.

The approved AbuseIPDB credential was validated with the provider's minimal documented `check` request. The implementation stores analyst-approved public-IP reputation evidence privately, records provenance in the case timeline, and renders an explicit approval control in Threat Intelligence. Deterministic integration tests, TypeScript checking, and the production build passed after this addition. VirusTotal and PhishTank remain unconnected because no approved credentials were supplied.

The approved VirusTotal credential was validated with a minimal public IP lookup. The implementation stores analyst-approved VirusTotal analysis statistics and concise network metadata privately alongside the separate AbuseIPDB result, records provider provenance in the case timeline, and includes both providers in real CSV/PDF exports. The final validation passed with 18 test files, 23 tests, two intentionally skipped live credential tests, TypeScript checking, and a production build. PhishTank remains the only unconnected reputation provider.

PhishTank is now available without a new application key through its officially documented verified-online feed. The implementation uses the HTTPS feed path, a descriptive user agent, a four-hour in-memory cache, compressed/decompressed size limits, and one analyst-approved extracted URL at a time. It keeps the selected URL result private to the case, records timeline provenance, and includes it in reports. A benign live feed validation passed, and the complete suite now has 20 passing test files and 25 passing tests with three opt-in live tests skipped during ordinary runs.

The updated Requirements preview was checked after the PhishTank implementation. It shows **22 available**, **0 needs connection**, and **0 not built**. The live intelligence row accurately distinguishes analyst-approved public source-IP checks for AbuseIPDB and VirusTotal from analyst-approved extracted URL checks against the PhishTank verified-online feed.

## Authorized real-email case validation — 23 August 2026

The user-authorized `/home/ubuntu/upload/sample2.eml` was processed through the protected browser workflow as private case **INV-2026-MT56Z7CG**. The evidence record retained the uploaded filename, SHA-256 `ac99e31610bc1759319092d037ec153390cc3fd0d12b3d1e2f4f7a4fa1c66b46`, sender/recipient metadata, and structural authentication state. It produced a structural score of `0/100` before the content assessment, with no extracted URL and no extracted public source IP. The normal owner-alert path correctly remained `not_needed` because the case was below the high-risk threshold.

The original GPT-based bounded review was truncated before complete JSON could be parsed. The repaired review uses the server-side `gemini-3-flash-preview` model with a bounded `1536`-token completion and a compact strict JSON schema. A protected, ownership-checked case retry was added so a missing assessment can be re-run without re-uploading or duplicating evidence. The authorized case successfully persisted an evidence-only `uncertain` assessment, model provenance, recommendations, a `7/100` low case score, and an `ai_content_analysis` timeline event. The test suite includes a regression contract for the protected retry route, persistence helper, bounded model configuration, and case-detail control.

Browser verification also saved an analyst note on the same private case. The Location Map view correctly blocked lookup with the explicit message that no usable public source IP had been extracted. Threat Intelligence correctly showed the email/hash indicators but made no AbuseIPDB, VirusTotal, or PhishTank request because the case has neither an applicable public IP nor URL. These no-data protections are successful outcomes for this evidence; map enrichment, provider happy paths, and high-risk alert delivery remain untested with a real case because the authorized email does not contain the required indicators or risk signals. No synthetic email, case, IOC, reputation response, or location was created.

The signed-in browser then rechecked the in-app **Requirements** view. It displayed **22 available**, **0 needs connection**, and **0 not built**. The AI email-threat row continued to describe a bounded server-side, evidence-only review; the provider row continued to distinguish analyst-approved public-IP checks from analyst-approved URL matching. The checklist did not need a status change because the repair makes an already-listed feature reliable rather than adding a new requirement capability.

## Authorized high-risk test email — 23 August 2026

The newly supplied `SIH26106_HighRisk_Test.eml` was uploaded through the protected browser workflow and stored as private case **INV-2026-MT57VP69**. It produced a `47/100` medium case score with a persisted Gemini phishing assessment and an `ai_content_analysis` timeline event. The saved case includes the submitted URL, three documentation-range IP indicators, attachment-name evidence, email indicators, and its SHA-256. The supplied message has an early header/body boundary, so its later `From`, `To`, SPF, DKIM, DMARC, and attachment MIME declarations are correctly treated as untrusted body content rather than trusted parsed headers; the AI review may describe this body evidence but does not make external reputation claims.

During inspection, all three supplied IPs were recognized as RFC 5737 documentation ranges (`192.0.2.0/24`, `198.51.100.0/24`, and `203.0.113.0/24`). The shared public-IPv4 guard was hardened and tested to reject these ranges before geolocation, AbuseIPDB, or VirusTotal requests. This prevents a user-supplied test message from producing an inappropriate external lookup. The case is therefore suitable for structural, AI, evidence, timeline, and blocked-enrichment validation, but not for real-public-IP provider or map happy-path validation.

Browser verification confirmed that Location Map now presents **No usable public source IP** and keeps its approval action disabled for this case. Threat Intelligence labels the same source as ineligible and removes AbuseIPDB/VirusTotal approval actions, while retaining the IP indicators as private evidence. The analyst-approved PhishTank workflow was exercised for the one extracted URL; it persisted a `safe` result stating that no verified online match was found in the cached feed. The high-risk owner-alert path did not run: despite the user-supplied filename, the protected parser correctly treated the post-boundary authentication lines as body content, leaving a `47/100` medium score below the configured `60` alert threshold. No public-IP lookup, provider-IP check, or fabricated evidence was created.

This browser check specifically confirms that RFC 5737 documentation-only addresses are blocked in both the Location Map and Threat Intelligence interfaces before a geolocation, AbuseIPDB, or VirusTotal approval can be issued.

The signed-in dashboard was rechecked after ingestion. It showed two private saved analyses, including **INV-2026-MT57VP69** at `47/100` medium severity and `100%` AI confidence, while the high-risk count remained `0` because the configured alert threshold is `60` or higher. This confirms the dashboard does not label a sub-threshold test case as high risk or claim a high-risk owner alert.

The final regression pass after the eligibility fix completed with **22 passing test files / 27 passing tests** and **3 intentionally skipped opt-in live-provider tests**. TypeScript checking and the production build also completed successfully. The build retains the pre-existing non-blocking runtime asset-resolution and bundle-size warnings.

## Material You workspace redesign — 25 August 2026

The authenticated workspace was restyled with a scoped Material You layer while the public landing route remained unchanged. The signed-in shell now uses rounded tonal surfaces, a violet seed palette, Roboto typography, pill-shaped navigation and actions, paired light/dark semantic tokens, and responsive mobile presentation. Desktop and mobile screenshots confirmed that the workspace adapts at the 800px breakpoint while the landing page keeps its previous Signal Furnace presentation. No email-analysis procedure, evidence record, route, authorization guard, AI boundary, or provider workflow was changed by this visual-only redesign.

Rendered previews confirmed the redesigned Dashboard, Threat Intelligence, Location Map, and Requirements views in both applicable light/dark states. The existing theme persistence contract remains covered by test; a fresh authenticated browser session is still required for a post-redesign manual toggle-and-reload check because the available interactive session redirected to OAuth during validation.
